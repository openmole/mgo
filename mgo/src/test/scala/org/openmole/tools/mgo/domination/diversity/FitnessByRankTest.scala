/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package org.openmole.tools.mgo.domination.diversity

import org.junit._
import org.openmole.tools.mgo.domination.diversity.FitnessByRank._

import Assert._
import org.openmole.tools.mgo.model.MultiGoal


class FitnessByRankTest {

  @Before
  def setUp: Unit = {
  }

  @After
  def tearDown: Unit = {
  }

  @Test
  def speed = {

//    val set = MultiGoal.buildDouble(5, 1000)
//    
//    val time = System.currentTimeMillis
//    val selected = selectByFitnessAndCrowding(set, 5, 500)
//
//    println("Time selectByFitnessAndCrowding " + (System.currentTimeMillis - time))
//    println("Size selectByFitnessAndCrowding " + selected.size)
//
//    val time2 = System.currentTimeMillis
//    val selected2 = selectByFitnessAndCrowdingPareto(set, 5, 500)
//
//    println("Time selectByFitnessAndCrowdingPareto " + (System.currentTimeMillis - time2))
//    println("Size selectByFitnessAndCrowdingPareto " + selected2.size)
//    
//    println(" " + selected.map{e=>e.goals(0).toDouble}.reduceLeft{ (l,r) => l +r} + " " + selected2.map{e=>e.goals(0).toDouble}.reduceLeft{ (l,r) => l + r})
  }
  
}
