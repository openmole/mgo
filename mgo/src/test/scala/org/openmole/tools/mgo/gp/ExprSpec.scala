/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package org.openmole.tools.mgo.gp

import org.scalatest.FlatSpec
import org.scalatest.matchers.ShouldMatchers
import org.scalatest.junit.JUnitRunner
import org.junit.runner.RunWith

@RunWith(classOf[JUnitRunner])
class ExprSpec extends FlatSpec with ShouldMatchers {
//  "Cos (Num (2.)) == Cos (Num (2.))" should " return true" in 
//    (Cos (Num (2.)) == Cos (Num (2.))) should equal (true)
    
  //"Sum3 (Num (1.), Num (1.)) == 5" should " return true" in 
  //  (Sum3 (Num (1.), Num (1.)).eval (Map.empty)) should equal (5)
    
  //"Sum2 (Num (1.), Num (1.)) == 5" should " return true" in
  //  (Sum2 (Num (1.), Num (1.)).eval (Map.empty)) should equal (4)
    
  //Sum2 (Sum3 (Num (0), Num (0)), Num (0)).eval (Map.empty) should equal (5)
}
